# Virtual environments
.venv/

# Data
data/raw/*
!data/raw/.gitkeep

# Models and checkpoints
*.pkl

# Jupyter
.ipynb_checkpoints/

# Python
__pycache__/

# OS
.DS_Store

# CatBoost training cache
catboost_info/

# Claude Code local permissions cache (per-machine, not work product)
.claude/settings.local.json

# Office lock files (PowerPoint creates ~$file.pptx while open)
~$*

# Processed data
data/processed/*
!data/processed/.gitkeep
data/pivot/*
!data/pivot/.gitkeep

# Reports output — deliverables ARE tracked; only large intermediates excluded
# (figures, PDFs, and decks are committed so the README badges + decks render on GitHub)

# Submissions
submissions/*
!submissions/.gitkeep
